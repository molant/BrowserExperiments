

$(document).ready(function () {
   
    
    //INIT Scroller
    var scroller = new Scroller({
       container: $("#dataList"),
       fps: 60,
       direction: "x",
       showScrollBars: true           
    });
    
    scroller.create();
     
  
});
