<div class="span16">
	<header class="page-header">
		<h2>Column gap</h2>
	</header>
</div>
<div class="span-two-thirds">
	<div class="alert-message block-message info">
		<p>The <code>column-gap</code> property sets the gap between columns. If there is a column rule between columns, it will appear in the middle of the gap.</p>
		<p><span class="label">dafault</span> The <code>normal</code> value is UA-specific. A value of <code>1em</code> is suggested.</p>
		<p><span class="label notice">Note</span> Column gaps cannot be negative.</p>
	</div>
</div>
<div class="span-one-third">
	<h3>Code example</h3>
	<div class="code">
		<pre class="prettyprint"><code class="language-css">.example {
	column-count: 3;
	column-gap: 40px;
}</code></pre>
	</div>
</div>

<div class="span16">
	<?php include 'includes/columns/example.php'; ?>
</div>